apiVersion: v1
kind: Service
metadata:
  name: erpserver
spec:
  selector:
    app: erpserver
  type: NodePort
  ports:
    - name: erpserver
      port: 8069
      targetPort: 8069
    - name: ssh
      port: 22
      targetPort: 22
